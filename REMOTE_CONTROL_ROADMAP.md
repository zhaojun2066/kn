# 远程控制产品路线图

> 产品定位：**kn = AI CLI 的远程控制方案**。不在电脑旁边时，用手机继续 Claude Code / Codex 工作。
>
> 运行配置切换是顺手做的事，远程控制才是核心差异。

---

## 现状评估

### 已完成 ✅

| 能力 | 说明 |
|------|------|
| 设备绑定 | QR 码 + 6 位码两条路径，token 原子存储，WSS 重连+指数退避 |
| 会话生命周期 | create → start → running → end 全流程，电脑端 + cloud + iOS 三端协同 |
| 会话 | PTY 输出流 + input/ctrl 输入，seq 确认，去重，限流，replay |
| 多会话 | 10 上限，iOS 多 Tab 切换 |
| 崩溃恢复 | session.json 持久化，重启自动恢复，重新同步到云端 |
| 跨节点中继 | Redis Pub/Sub 多节点消息路由 |
| 心跳/僵尸清理 | CLI 心跳 15s，cloud 30s 扫描，超时自动结束 |
| 电脑端面板 | 手机远程控制面板支持本地/手机可继续会话管理、批量操作、绑定/兑换 |

### 已有基础设施但未接通 ⚠️

| 能力 | 状态 |
|------|------|
| APNs 推送 | token 注册 + provider 已实现，但未在任何事件中触发推送 |
| 语音输入 | `SpeechRecognizer.swift` 存在，未深度接入终端输入流 |
| 会话 Relay 模式 | agent 支持 `Relay` 类型会话（桌面 PTY → agent 转发），但 iOS 还不能接管已有桌面会话 |

### 缺失 ❌

| 能力 | 说明 |
|------|------|
| 远程文件访问 | iOS 完全看不到电脑端项目文件 |
| 会话接力 | 电脑端 → 移动端切换 PTY 会话 |
| 后台保活 | iOS 退后台即断连 |

---

## 路线图

### P0 — 故事闭环（必须补）

这三个不做，产品故事讲不圆。

#### 1. 推送通知

**场景**：拿出手机发起 Claude Code 任务，切出去看别的。任务跑完了，推送一条通知："✅ 任务完成，点击查看结果"。

**实现**：
- `kn-cloud`: 在 `session_ended` 事件处理中调用 `ApnsService.push()`，推送标题 + 摘要
- `kn-ios`: `AppDelegate` 收到推送后跳转到对应会话详情
- 额外触发点：`session_created`（会话已就绪）、长任务进度（可选）

#### 2. 远程文件访问

**场景**：Claude 帮你生成了代码/文档/图表，在手机上直接预览、分享。

**实现**：
- `kn-agent`: 新增协议消息 `list_files`（目录浏览）、`read_file`（内容读取）、`download_file`（Base64 传输小文件）
- `kn-ios`: 文件树 → 代码预览（语法高亮）→ Share Sheet 分享
- 安全边界：限制在 `cwd` 及其子目录，不暴露整个文件系统
- 图片/PDF 直接渲染，代码文件带行号和语法高亮

#### 3. 会话接力（Handoff）

**场景**：在电脑端开了一个 Claude Code 会话，出门后手机**直接接手**，同一个 PTY，继续聊。

**实现**：
- 所有电脑端会话默认以 Relay 模式运行（PTY 由电脑端持有，agent 做输出中继）
- iOS 连接时发送 `attach_session`（而非 `start_session`），agent 将该会话的 WSS 输出流扇出到 iOS
- 同时只能有一个输入端 active（电脑端 vs iOS），切换时通过 `ctrl` 消息协商
- 配套：当 iOS 接入时，电脑端弹出提示“会话已被手机接管”

---

### P1 — 体验显著提升（应该补）

#### 4. iOS 后台保活

**场景**：发起任务后锁屏，任务继续跑，回来时输出完整展示。

**实现**：
- 短期：`beginBackgroundTask` 争取 30 秒缓冲
- 长期：APNs 静默推送（`content-available: 1`）唤醒 app → 重建 WSS → agent replay 期间输出
- 任务运行中每隔 N 秒发心跳推送，确保 app 不被系统挂起（视 Apple 限制而定）

#### 5. 会话摘要卡片

**场景**：任务跑完，不用翻终端原始输出，直接看结构化结果卡片。

**实现**：
- `kn-agent` 在 `session_ended` 时收集：
  - 会话时长、Token 消耗（如果可获取）
  - 工作目录、使用的 tool
  - 尝试从 PTY 日志中提取变更文件列表
- 作为 `session_ended` 的扩展字段下发
- `kn-ios` 用卡片 UI 渲染（类似于 GitHub 的 commit 卡片），而非扔在终端流里

#### 6. 语音输入优化

**场景**：走路上不方便打字，语音说需求，Claude Code 接着干。

**实现**：
- 完善 `SpeechRecognizer` → 终端输入流管道
- 语音识别结果展示 + 发送前可编辑
- 支持"语音快捷指令"（预设 prompt 模板，一句话触发）
- 考虑直接在终端输入栏旁放一个明显的麦克风按钮

#### 7. 会话模板 / 快捷指令

**场景**：一键启动"继续上次项目"或"daily standup 准备"，不用每次填参数。

**实现**：
- iOS 端预设模板：`tool` + `cwd` + `profile` + 初始 prompt 的组合
- 模板来源：用户手动创建 + 自动从最近会话中提取
- "继续上次"模板自动填入最近一次会话的所有参数
- 可选的初始 prompt："帮我 review 今天的改动" / "运行测试并修 bug" 等

---

### P2 — 差异化竞争力（锦上添花）

#### 8. 多 Mac 管理

**场景**：一台 Mac mini 在家，一台 MacBook 随行，手机端选择连哪台。

**实现**：
- 绑定流程支持多台 Mac（`device_id` 区分）
- iOS 端设备列表展示：每台 Mac 的在线状态、当前活跃会话数
- 一台 iOS 可同时连多台 Mac（多 WSS 连接），Tab 切换
- 从已有架构扩展（kn-cloud 已支持多节点）

#### 9. 会话分享

**场景**：把当前会话链接发给同事，对方在浏览器里实时看到终端输出（只读）。

**实现**：
- 生成只读分享链接（短链 + 可选密码 + 可选过期时间）
- 接收方打开网页即可看到实时终端流（只读，无输入权限）
- 类似 tmate / VS Code Live Share 的终端版
- 服务端可复用已有 WSS 中继能力，额外做一个 Web 前端

#### 10. 定时任务 / 自动化

**场景**："每天凌晨 2 点帮我跑 `ai claude project-x`，跑完推通知给我"。

**实现**：
- `kn-agent` 内置 cron 调度器
- 用户通过 Desktop 或 iOS 配置：时间 + tool + profile + cwd + prompt
- 执行完推送结果通知
- 失败重试 + 错误通知
- 这是一个从"被动连接"到"主动帮手"的质变点

#### 11. 会话搜索

**场景**：三天前让 Claude 帮忙写过一段代码，不记得在哪个会话里了，搜一下。

**实现**：
- agent 端对 PTY 输出日志建全文索引
- iOS 端搜索 UI：搜关键词，返回匹配的会话 + 上下文片段
- 点击跳转到该会话的 replay 位置

#### 12. 剪贴板同步

**场景**：手机上看到一段 prompt，复制 → Mac 上的终端自动粘贴。

**实现**：
- 通过 WSS 消息同步剪贴板（字符串内容，非二进制）
- 可选：自动同步 vs 手动触发
- macOS 端用 `NSPasteboard` 写入，agent 负责接收

---

## 优先级矩阵

```
                    影响力
              高              低

         │ P0-1 推送     │ P1-5 摘要卡片  │
   低    │ P0-2 文件访问  │ P1-7 会话模板  │
         │ P0-3 会话接力  │               │
成本    ──────────────────────────────────
         │               │ P2-8 多Mac    │
   高    │ P1-4 后台保活  │ P2-9 分享     │
         │               │ P2-10 定时任务 │
         │               │ P2-11 搜索    │
         │               │ P2-12 剪贴板   │
```

**执行建议**：先集中资源把 P0 三件事做完，这三点做完产品故事就闭环了——"离开 Mac 也能完整地写代码"。P1 在 P0 之后做，P2 量力而行。
