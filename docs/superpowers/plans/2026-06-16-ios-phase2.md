# iOS Phase 2 — 推送处理 + Sessions 历史 + 语音输入

> ⚠️ **权威版本在 `../kn-ios/docs/2026-06-16-ios-phase2.md`**。此文件为 kn monorepo 中的只读副本，修改请到 kn-ios repo。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use `- [ ]` checkbox syntax.

**Goal:** 推送通知处理（前台/后台/点击）、Sessions 历史列表与消息查看、语音输入集成、iPad 基本适配。

**Prerequisites:** iOS Phase 1 完成。

---

### Task 9: 推送通知处理

**Files:**
- Modify: `Services/PushManager.swift`
- Modify: `App/KnApp.swift`

- [ ] **Step 1: PushManager — 前台/后台/点击处理**

```swift
import UserNotifications

@MainActor
final class PushManager: NSObject, ObservableObject {
    static let shared = PushManager()
    var deviceToken: String?
    @Published var lastNotification: PushEvent?

    struct PushEvent: Identifiable {
        let id = UUID()
        let type: PushEventType
        let title: String
        let body: String
        let timestamp: Date
    }

    enum PushEventType {
        case aiComplete      // AI 执行完成
        case aiNeedConfirm   // AI 需要确认
        case agentCrash      // Agent crash
        case trialExpiring   // 试用即将到期
        case unknown
    }

    func handleNotification(_ userInfo: [AnyHashable: Any]) {
        guard let aps = userInfo["aps"] as? [String: Any],
              let alert = aps["alert"] as? [String: Any] else { return }

        let typeStr = userInfo["kn_type"] as? String ?? ""
        let eventType: PushEventType = switch typeStr {
            case "ai_complete": .aiComplete
            case "ai_confirm": .aiNeedConfirm
            case "agent_crash": .agentCrash
            case "trial_expiring": .trialExpiring
            default: .unknown
        }

        lastNotification = PushEvent(
            type: eventType,
            title: alert["title"] as? String ?? "",
            body: alert["body"] as? String ?? "",
            timestamp: Date()
        )
    }

    func registerDeviceToken() {
        guard let token = deviceToken else { return }
        Task {
            let _ : [String: String] = try await APIClient.shared.post(
                "/api/v1/push/register",
                body: ["device_token": token]
            )
        }
    }
}
```

- [ ] **Step 2: UNUserNotificationCenterDelegate 实现**

在 `KnApp.swift` 的 `AppDelegate` 中：

```swift
// 前台收到推送：展示 banner
func userNotificationCenter(_ center: UNUserNotificationCenter,
                            willPresent notification: UNNotification,
                            withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
    PushManager.shared.handleNotification(notification.request.content.userInfo)
    completionHandler([.banner, .sound])
}

// 用户点击推送 → 打开 App 对应页面
func userNotificationCenter(_ center: UNUserNotificationCenter,
                            didReceive response: UNNotificationResponse,
                            withCompletionHandler completionHandler: @escaping () -> Void) {
    PushManager.shared.handleNotification(response.notification.request.content.userInfo)
    // 根据 kn_type 导航到对应 session
    completionHandler()
}

// APNs token 注册成功
func application(_ application: UIApplication,
                 didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
    let token = deviceToken.map { String(format: "%02x", $0) }.joined()
    PushManager.shared.deviceToken = token
    PushManager.shared.registerDeviceToken()
}
```

- [ ] **Step 3: Commit**

```bash
git commit -m "feat: push notification handling (foreground/background/tap)"
```

---

### Task 10: Sessions 历史列表 + 消息查看

**Files:**
- Create: `Sessions/SessionListView.swift`
- Create: `Sessions/SessionDetailView.swift`
- Create: `Sessions/SessionViewModel.swift`

- [ ] **Step 1: SessionViewModel**

```swift
@MainActor
final class SessionViewModel: ObservableObject {
    @Published var sessions: [Session] = []
    @Published var isLoading = false

    func fetchSessions() async {
        isLoading = true
        defer { isLoading = false }
        do {
            let resp: [Session] = try await APIClient.shared.get("/api/v1/session/list")
            sessions = resp
        } catch {
            print("获取 sessions 失败: \(error)")
        }
    }
}
```

- [ ] **Step 2: SessionListView**

```swift
struct SessionListView: View {
    @StateObject private var viewModel = SessionViewModel()

    var body: some View {
        NavigationStack {
            List(viewModel.sessions) { session in
                NavigationLink(destination: SessionDetailView(session: session)) {
                    HStack {
                        statusIcon(session.status)
                        VStack(alignment: .leading) {
                            Text("\(session.tool) — \(session.cwd ?? "~")")
                            Text(session.startedAt).font(.caption).foregroundColor(.secondary)
                        }
                    }
                }
                // 右滑操作
                .swipeActions(edge: .trailing) {
                    if session.status == "running" {
                        Button("关闭") { /* 发 kill_session */ }
                            .tint(.red)
                    }
                }
            }
            .navigationTitle("Sessions")
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    Button(action: { Task { await viewModel.fetchSessions() } }) {
                        Image(systemName: "arrow.clockwise")
                    }
                }
            }
            .task { await viewModel.fetchSessions() }
        }
    }

    @ViewBuilder
    func statusIcon(_ status: String) -> some View {
        switch status {
        case "running":   Circle().fill(Color(hex: "#00e676")).frame(width: 8, height: 8)
        case "completed": Circle().fill(Color(hex: "#2979ff")).frame(width: 8, height: 8)
        case "failed":    Circle().fill(Color(hex: "#ff3d00")).frame(width: 8, height: 8)
        default:          Circle().fill(Color.gray).frame(width: 8, height: 8)
        }
    }
}
```

- [ ] **Step 3: SessionDetailView**

```swift
struct SessionDetailView: View {
    let session: Session
    @State private var messages: [SessionMessage] = []

    var body: some View {
        List(messages) { msg in
            VStack(alignment: .leading) {
                HStack {
                    Text(msg.direction == "inbound" ? "📤" : "🤖")
                        .font(.caption)
                    Text(msg.createdAt).font(.caption2).foregroundColor(.secondary)
                }
                Text(msg.content).font(.system(size: 14, design: .monospaced))
            }
        }
        .navigationTitle("Session \(session.id)")
        .task {
            await fetchMessages()
        }
    }

    func fetchMessages() async {
        do {
            messages = try await APIClient.shared.get(
                "/api/v1/session/\(session.id)/messages")
        } catch {
            print("获取消息失败: \(error)")
        }
    }
}

struct SessionMessage: Codable, Identifiable {
    let id: Int
    let seq: Int
    let direction: String
    let msgType: String
    let content: String
    let createdAt: String
}
```

- [ ] **Step 3.5: 将 SessionListView 加入 TabView**

修改 `App/KnApp.swift`，在 TabView 中新增第三个 tab：

```swift
TabView {
    TerminalView(viewModel: appState.terminalVM)
        .tabItem { Label("Terminal", systemImage: "terminal") }
    SessionListView()
        .tabItem { Label("Sessions", systemImage: "list.bullet") }
    DeviceListView()
        .tabItem { Label("Devices", systemImage: "laptopcomputer") }
}
```

- [ ] **Step 4: Commit**

```bash
git add Sessions/ App/KnApp.swift
git commit -m "feat: sessions history list, message detail view, and TabView integration"
```

---

### Task 11: 语音输入（iOS 原生 Speech）

**Files:**
- Modify: `Terminal/TerminalView.swift` (添加麦克风按钮)
- Create: `Terminal/SpeechRecognizer.swift`

- [ ] **Step 1: SpeechRecognizer**

```swift
import Speech

@MainActor
final class SpeechRecognizer: ObservableObject {
    @Published var transcript = ""
    @Published var isRecording = false

    // 根据系统语言自动选择，支持中/英/日
    private let recognizer = SFSpeechRecognizer(locale: Locale(identifier: 
        Locale.preferredLanguages.first?.starts(with: "zh") == true ? "zh-CN" :
        Locale.preferredLanguages.first?.starts(with: "ja") == true ? "ja-JP" : "en-US"
    ))
    private var recognitionRequest: SFSpeechAudioBufferRecognitionRequest?
    private var recognitionTask: SFSpeechRecognitionTask?
    private let audioEngine = AVAudioEngine()

    func requestAuthorization() async -> Bool {
        await withCheckedContinuation { cont in
            SFSpeechRecognizer.requestAuthorization { status in
                cont.resume(returning: status == .authorized)
            }
        }
    }

    func startRecording() throws {
        recognitionTask?.cancel()
        recognitionTask = nil

        let audioSession = AVAudioSession.sharedInstance()
        try audioSession.setCategory(.record, mode: .measurement, options: .duckOthers)
        try audioSession.setActive(true)

        recognitionRequest = SFSpeechRecognitionRequest()
        guard let request = recognitionRequest else { return }
        request.shouldReportPartialResults = true

        recognitionTask = recognizer?.recognitionTask(with: request) { [weak self] result, error in
            if let result = result {
                self?.transcript = result.bestTranscription.formattedString
            }
        }

        let inputNode = audioEngine.inputNode
        let recordingFormat = inputNode.outputFormat(forBus: 0)
        inputNode.installTap(onBus: 0, bufferSize: 1024, format: recordingFormat) { buffer, _ in
            request.append(buffer)
        }

        audioEngine.prepare()
        try audioEngine.start()
        isRecording = true
    }

    func stopRecording() {
        audioEngine.stop()
        audioEngine.inputNode.removeTap(onBus: 0)
        recognitionRequest?.endAudio()
        recognitionTask?.cancel()
        isRecording = false
    }
}
```

- [ ] **Step 2: TerminalView 麦克风按钮**

在 `InputAccessoryBar` 的 `[🎤]` 按钮中：

```swift
struct InputAccessoryBar: View {
    @StateObject private var speech = SpeechRecognizer()

    var body: some View {
        HStack {
            // ... 现有按钮 ...

            // [🎤] 语音输入
            Button(action: {
                Task {
                    if speech.isRecording {
                        speech.stopRecording()
                        viewModel.inputText += speech.transcript
                    } else {
                        if await speech.requestAuthorization() {
                            try? speech.startRecording()
                        }
                    }
                }
            }) {
                Text(speech.isRecording ? "🔴" : "🎤")
            }
        }
    }
}
```

- [ ] **Step 3: Commit**

```bash
git commit -m "feat: speech-to-text via iOS native Speech framework"
```

---

## iOS Phase 2 完成检查点

- [x] 推送通知 (前台 banner + 后台 + 点击导航)
- [x] Sessions 历史列表 + 右滑关闭 + 消息详情
- [x] 语音输入 (iOS Speech 原生，结果填入输入栏)

**全部 iOS Phase 完成后的能力**：
- [x] 登录/注册 + Keychain JWT
- [x] WSS 实时通信
- [x] TerminalView (WKWebView + xterm.js + 直通模式)
- [x] 设备列表 + 扫码绑定
- [x] 推送通知 (APNs)
- [x] Sessions 历史 + 消息查看
- [x] 语音输入

尚未实现（后续 Phase 补充）：
- [ ] Certificate pinning (设计文档 §6.6)：利用 iOS ATS + NSAppTransportSecurity 配置固定证书指纹，防止 WSS 中间人劫持。Phase 1-2 先依赖 TLS 1.3 + Apple ATS，生产部署前补齐为硬编码 pin hash。
- [ ] iOS 任务切换器敏感信息模糊处理 (设计文档 §6.6)：检测 `UIApplication.didEnterBackgroundNotification` → 覆盖模糊层
- [ ] iPad 横屏适配
