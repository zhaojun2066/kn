# KN v1.2.0 上线前问题清单

> 状态：持续维护。已修复项目标记为“已修复”；未完成项目仍不得作为上线通过依据。
>
> 范围：KN 桌面端、Agent、kn-cloud API/WSS、kn-admin、GitHub Actions 和自有服务器发布链路。
>
> 本文件只记录问题和验收要求，不代表问题已经修复。每项完成后补充修复提交、测试结果和完成日期。

## 一、必须优先修复

### 1. 自解绑后旧 WSS 连接可能仍然有效

> 状态：代码已修复（Cloud 提交后发布 `kick_device`；Agent 取消当前 WSS、清空 sender、关闭全部本地远控；需 Cloud 部署和真机验收）。

- 级别：P0，高危安全问题
- 涉及文件：
  - `agent/src/ipc.rs:1724`
  - `agent/src/main.rs:860`
  - `kn-cloud/kn-cloud-api/src/main/java/dev/kn/cloud/api/service/DeviceService.java:788`
  - `kn-cloud/kn-cloud-ws/src/main/java/dev/kn/cloud/ws/component/RedisSubscriber.java:164`
- 现象：
  - Agent 调用 `self-unbind` 后删除本地 token，但没有取消当前 WSS 任务。
  - Cloud 解绑时删除数据库 token 和 Redis 会话数据，但没有向 WSS 发布 `kick_device`。
  - 已建立的 WebSocket 连接可能在真正断开前继续存在。
- 影响：用户已经确认解除本机绑定后，手机可能仍能继续操作已有远程会话。
- 修复要求：
  1. Cloud 解绑成功后发布带 `machineId` 的 `kick_device` 控制消息。
  2. WSS 收到后立即关闭 Agent WebSocket 和相关远程会话。
  3. Agent self-unbind 主动取消 WSS `CancellationToken`，清空 outgoing sender。
  4. WSS 任务退出后不得自动重连。
  5. 本地会话全部关闭远控状态后，才返回解绑成功。
- 验收标准：解绑接口返回成功后，手机立即无法发送输入；Cloud WSS 连接关闭；Agent 不再重连；重新绑定前状态保持未绑定。

### 2. 下载取消按钮没有真正取消下载

> 状态：已修复（Rust 取消标记、前端取消命令、失败清理和事件监听清理已加入；需真机验收）。

- 级别：P1，用户体验和行为错误
- 涉及文件：
  - `desktop/src/App.tsx:2111`
  - `desktop/src/app/useUpdateCheck.ts:35`
  - `desktop/src-tauri/src/commands/network.rs:32`
- 现象：点击“取消”只关闭更新窗口和重置前端状态，Rust 后台下载任务仍继续执行。
- 影响：用户以为已经取消，但下载仍占用网络和磁盘；下载结束后还可能自动打开 DMG。
- 修复要求：
  1. 为下载任务增加可取消的任务 ID 或 `CancellationToken`。
  2. 前端取消时通知 Rust 停止请求、关闭响应流并删除临时文件。
  3. 取消后不得执行 SHA-256 校验完成流程，也不得打开 DMG。
  4. 下载失败和取消都要解除事件监听。
- 验收标准：下载中点击取消后，网络请求停止、临时文件删除、不会打开安装包；重试可以重新开始。

## 二、发布前必须修复

### 3. Admin 上传失败时页面没有错误反馈

> 状态：已修复（页面已捕获创建、发布、撤回、回滚错误；需部署后验收）。

- 级别：P1
- 文件：`kn-cloud/kn-admin-web/src/pages/DesktopReleasesPage.vue:9-10`
- 现象：`create()` 和 `action()` 只有 `try/finally`，没有 `catch`；接口失败时页面不显示服务端错误。
- 影响：管理员不知道是签名、公钥、哈希、版本还是服务器配置错误，容易重复提交或误以为操作成功。
- 修复要求：
  1. 增加页面级错误提示区域。
  2. 捕获 `AdminApiError`，显示后端返回的中文消息。
  3. 上传失败时保留表单和已选文件，不自动关闭弹窗。
  4. 发布、撤回、回滚失败时保留当前列表并提示原因。
  5. 操作成功后显示明确成功状态，期间禁止重复点击。
- 验收标准：模拟签名错误、版本重复、网络 500、权限失效，页面均有明确反馈且不会误关闭或重复提交。

### 4. staging 目录在数据库或审计失败时不会清理

> 状态：已修复（失败清理、启动时清理超过 24 小时的 staging、清理失败日志已加入）。

- 级别：P1，服务器资源泄漏
- 文件：`kn-cloud/kn-admin-api/src/main/java/dev/kn/admin/service/DesktopReleaseAdministrationService.java:42-55`
- 现象：文件写入 staging 后，如果 `mapper.insert()` 或 `audit.record()` 抛异常，清理逻辑不会执行。
- 影响：多次失败上传会遗留完整 DMG，占用磁盘，最终可能导致后续发布失败。
- 修复要求：
  1. staging 创建后，数据库插入、审计和原子移动全部纳入统一异常清理逻辑。
  2. 任意失败都删除 staging 目录。
  3. 增加启动时或定时清理过期 `.staging` 目录的机制。
  4. 清理失败必须写入日志并告警，不能静默吞掉。
- 验收标准：分别模拟文件复制失败、数据库失败、审计失败、原子移动失败，staging 均不会残留。

### 5. CI 没有验证内置 Agent 的产品版本

> 状态：已修复（挂载 DMG 后执行内置 Agent `--version` 并与 App 版本严格比较）。

- 级别：P1，发布产物错误
- 文件：`.github/workflows/build-desktop.yml:168-184`
- 现象：CI 检查 Agent 存在、可执行和架构，但没有检查 `kn-agent --version` 是否等于 App 版本。
- 影响：可能把错误版本的 Agent 打进 DMG，桌面启动时不断替换或与 Cloud 协议不兼容。
- 修复要求：挂载 DMG 后执行内置 Agent 的 `--version`，断言等于 `APP_VERSION`。
- 验收标准：人为放入错误版本 Agent 时 CI 必须失败；版本一致时通过。

### 6. CI 没有单独验证内置 Agent 签名

> 状态：已修复（增加 `codesign --verify --strict --verbose=2`）。

- 级别：P1，发布安全门禁不完整
- 文件：`.github/workflows/build-desktop.yml:178`
- 现象：目前依赖 `codesign --verify --deep` 检查整个 App，没有显式验证 Agent 文件自身签名。
- 修复要求：增加：

```bash
codesign --verify --strict --verbose=2 \
  "$APP/Contents/Resources/resources/kn-agent"
```

- 验收标准：对 Agent 签名破坏后，CI 必须失败，不得生成 release-candidate。

## 三、Cloud 和协议问题

### 7. Java prerelease SemVer 比较错误

> 状态：已修复。v1.2.0 只接受正式 `x.y.z`，Admin 和 Cloud 均不接受 prerelease。

- 级别：P2
- 文件：`kn-cloud/kn-cloud-api/src/main/java/dev/kn/cloud/api/service/DesktopReleaseService.java:68-72`
- 现象：使用字符串 `compareTo()` 比较 prerelease，例如 `alpha.10` 和 `alpha.2` 顺序错误。
- 影响：未来如果允许 prerelease 版本，可能错误判断是否有更新。
- 验收标准：覆盖正式版本比较、非法版本返回 400、版本回退拒绝。

### 8. 公共配置白名单范围过宽

> 状态：已修复。仅 `/api/v1/app-config/desktop-release` 匿名开放。

- 级别：P2，安全边界问题
- 文件：`kn-cloud/kn-cloud-api/src/main/java/dev/kn/cloud/api/config/AuthFilter.java:58-65`
- 现象：放行整个 `/api/v1/app-config/**`，而实际只需要公开桌面发布接口。
- 影响：以后新增配置接口时可能被意外匿名访问。
- 修复要求：只放行 `/api/v1/app-config/desktop-release`，并为该路径增加测试。
- 验收标准：桌面发布接口匿名可访问，其他 app-config 接口必须经过认证。

### 9. currentVersion 非法输入处理不够严格

> 状态：已修复。Cloud 对非法 `currentVersion` 返回统一 400；只接受 `x.y.z`。

- 级别：P2
- 文件：`kn-cloud/kn-cloud-api/src/main/java/dev/kn/cloud/api/service/DesktopReleaseService.java:58-63`
- 现象：非法的 `currentVersion` 被当作低版本处理，可能返回更新，而不是返回参数错误。
- 影响：不符合严格 SemVer 契约，可能掩盖客户端版本或接口调用错误。
- 修复要求：非法版本返回明确的 400；合法的 `0.0.0` 仍能正常查询。
- 验收标准：覆盖空值、`1.2`、`v1.2.0`、字母版本、正常 SemVer。

## 四、桌面更新和 Agent 运行问题

### 10. 更新流程需要补强 `mandatory` 处理

> 状态：已修复（强制升级隐藏关闭/稍后/取消入口，显示强制升级原因；普通更新仍可稍后）。

- 级别：P2
- 涉及文件：
  - `desktop/src-tauri/src/commands/release.rs:17-27`
  - `desktop/src/app/useUpdateCheck.ts:8-16`
  - `desktop/src/App.tsx:2098-2115`
- 现象：Cloud 返回 `mandatory`，Rust 虽然解析，但前端更新状态没有完整使用强制升级语义。
- 影响：未来协议不兼容时，客户端可能仍允许用户跳过强制更新。
- 修复要求：强制升级时隐藏“稍后”，远控入口禁用，并显示升级原因。
- 验收标准：`mandatory=true` 时不能跳过；普通更新仍可稍后处理。

### 11. Agent 修复或重启失败后的状态需要统一

> 状态：已修复（修复安装先写临时文件并备份旧 Agent；launchd 注册失败恢复旧文件并返回明确错误）。

- 级别：P2
- 涉及文件：
  - `desktop/src/components/AgentPanel.tsx:351-356`
  - `desktop/src-tauri/src/commands/agent_control.rs`
  - `desktop/src-tauri/src/lib.rs:331-484`
- 检查重点：
  - Agent 二进制替换成功但 launchd 重启失败时，页面必须明确显示“已替换但未启动”。
  - 修复过程失败时临时文件不能覆盖现有可用 Agent。
  - launchd plist 写入失败时不能误报修复成功。
  - 生产 Agent 的云端地址必须继续来自包内 `update.json`。
- 验收标准：模拟权限失败、文件缺失、launchd bootstrap 失败，页面均给出可执行恢复动作。

## 五、Admin 和发布链路补充问题

### 12. Admin 发布接口需要增加并发发布保护

> 状态：已修复（发布与回滚在事务内锁定当前公开记录，数据库唯一索引继续作为最终保护；需并发压测验收）。

- 级别：P2
- 文件：`kn-cloud/kn-admin-api/src/main/java/dev/kn/admin/service/DesktopReleaseAdministrationService.java:58-62`
- 风险：两个管理员同时发布草稿时，可能同时清空 current pointer 并更新不同版本。
- 修复要求：使用数据库事务锁或唯一指针更新策略，保证任何时刻只有一个公开版本。
- 验收标准：并发发布测试后，数据库只有一条 `current_slot=1`，公开 API 只返回最终有效版本。

### 13. 发布接口需要验证文件大小和可用磁盘空间

> 状态：已修复（Admin 业务层限制单文件大小并预留磁盘空间）。

- 级别：P2
- 现状：Spring Multipart 已配置请求上限，但服务逻辑没有明确单文件业务上限、剩余空间检查和失败告警。
- 影响：大文件上传可能耗尽 staging 所在磁盘。
- 修复要求：增加业务大小上限、剩余空间检查、上传目录监控和清理告警。
- 验收标准：超过上限、空间不足时，在写入大量文件前明确拒绝。

### 14. Nginx 下载目录需要实机验证

> 状态：配置已修复（移除 `alias` 下不可靠的 `try_files`）；仍需线上用 curl 完成 Range、HEAD 和 404 验收。

- 级别：P2，部署风险
- 文件：`kn-cloud/deploy/nginx.conf:130-136`
- 检查重点：`alias` 与 `try_files` 组合、Range 请求、HEAD 请求、只读权限、目录穿越防护。
- 验收标准：

```bash
curl -I https://api.example.com/releases/1.2.0/kn-arm.dmg
curl -H 'Range: bytes=0-1023' -o /tmp/part.dmg https://api.example.com/releases/1.2.0/kn-arm.dmg
```

返回正确的 200/206、Content-Length、Accept-Ranges，非法路径返回 404。

## 六、文档和发布流程问题

### 15. 旧文档仍描述 GitHub 自动 Release

> 状态：已修复（AGENTS、README、RELEASE 已统一为仅生成候选 artifact，Cloud/Admin/官网人工部署）。

- 级别：P2，运维流程风险
- 涉及文件：`AGENTS.md`、`README.md`、`RELEASE.md`
- 现象：部分内容仍描述 GitHub Release、GitHub Pages 自动发布，与当前手动部署 Cloud、Admin、官网的方案不一致。
- 修复要求：统一改为：
  - GitHub Actions 只构建、签名、公证并上传 release-candidate artifact。
  - Cloud、WSS、Admin、官网由人工部署。
  - DMG 通过 kn-admin 上传、计算 SHA-256、发布。
- 验收标准：新成员只阅读仓库文档和总手册时，不会误走 GitHub Release 或自动部署流程。

### 16. 发布手册上传 JAR 使用通配符存在误装风险

> 状态：已修复（部署手册使用唯一运行 JAR 校验和精确安装步骤）。

- 级别：P2
- 文件：`发布与上线总手册.md:333-337`
- 现象：`kn-admin-api-*.jar` 可能匹配多个 JAR，导致 `install` 参数异常或安装错误文件。
- 修复要求：构建后明确选取唯一运行 JAR，或使用精确文件名并在部署前执行 `ls -l` 校验。
- 验收标准：部署命令不会匹配 sources、original 或其他附属 JAR。

## 七、测试和上线门禁

以下项目完成前，不应宣布 v1.2.0 完成：

- [ ] 自解绑后 Cloud/WSS/Agent 三方立即断开（代码已修复，待部署和真机复验）。
- [x] 下载取消真正停止任务且不会打开 DMG（待真机复验）。
- [x] Admin 所有失败路径都有页面反馈（待部署复验）。
- [ ] staging 失败清理和过期目录清理通过测试。
- [x] CI 校验 App 与 Agent 版本、架构、签名、公证（需下一次 Actions 实跑）。
- [ ] Cloud 发布接口通过版本回退、非法版本、架构选择测试。
- [ ] Admin 并发发布和回滚测试通过（代码已增加事务锁，待部署压测）。
- [ ] Nginx HTTPS、Range、HEAD 和 404 测试通过。
- [ ] ARM 与 Intel 真机完成首次安装、绑定、远控、断网重连、升级、解绑验收。
- [ ] 观察正式发布至少 24 小时，无异常增长的错误日志、staging 文件或下载失败。
