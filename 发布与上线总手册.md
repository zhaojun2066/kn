# KN 发布与上线总手册

本文只覆盖 KN 桌面端、kn-cloud API/WSS、kn-admin 和官网；`kn-ios` 暂不在范围内。

先记住两条原则：

1. GitHub Actions 生成已签名、公证的桌面候选包，并创建或更新同 tag 的 GitHub Release；绝不部署服务器。
2. GitHub Actions 构建完成后，发布人手动把两个 DMG 上传到 kn-admin；Admin 计算并保存 SHA-256，桌面端下载后校验 SHA-256。

> # ⚠️ 到期提醒：本手册涉及的凭据不是同一种“证书”
>
> **会过期：**
> 1. **Apple Developer ID Application 证书**：Apple 会给出明确的 `Expires` 日期。下载的 `.cer` 和导出的 `.p12` 都受这张证书的有效期约束；`.p12` 本身不会因为重新导出而延长有效期。
> 2. **服务器 HTTPS/TLS 证书**：例如 Nginx 使用的 Let's Encrypt 证书，通常约 90 天；必须配置自动续期，并在续期后 reload Nginx。
> 3. **Apple Developer Program 会员资格**：账号会员到期或被暂停，会影响签名、公证等发布能力；它不是 `.p12` 文件的有效期，但同样必须持续有效。
>
> **通常没有固定到期日，但会被撤销而失效：**
> - **App Store Connect API Key 的 `.p8`（文件 A）**：没有像证书那样的 `Expires` 日期；丢失原文件不能再次下载，只能新建 Key。Key 被撤销后，GitHub 公证立即失败。
> - **APNs Auth Key 的 `.p8`（文件 B）**：通常没有固定到期日；丢失不能补下载，只能新建 Key。Key 被撤销后，Cloud 推送立即失败。
>
> **没有独立到期日：**
> - `.p12` 导出密码、`KEYCHAIN_PASSWORD`、GitHub Secret、SHA-256、DMG 文件本身。它们不会自动过期，但泄露后必须主动轮换。
>
> **每次发布前必须检查：**在钥匙串访问中查看 `Developer ID Application` 的 `有效期至`；在服务器执行 `sudo certbot renew --dry-run`；在 Apple Developer/App Store Connect 确认账号和 Key 未被撤销。到期前至少提前 30 天更换证书并更新 GitHub Secrets。

---

## 一、先准备一次

这一节只在首次上线、换服务器、换域名、换发布人或轮换密钥时做。正常发新版本时直接从“二、发布桌面 KN”开始。

### 1. 确定三个域名

请先确定并替换下面示例：

| 名称 | 示例 | 用途 |
|---|---|---|
| API 域名 | `https://api.example.com` | REST API、WSS、DMG 下载 |
| Admin 域名 | `https://admin.example.com` | 管理后台网页和管理 API |
| 官网域名 | `https://www.example.com` | 用户下载官网 |

DNS 要先将上述域名的 A/AAAA 记录指向服务器公网 IP，之后再申请 HTTPS 证书。

### 2. Apple 开发者账号和 GitHub Secrets

1. 登录 <https://developer.apple.com/account>，确认 Apple Developer Program 为 Active，记录 Team ID。
2. 打开 Mac“钥匙串访问” -> 菜单“钥匙串访问” -> “证书助理” -> “从证书颁发机构请求证书”。
   - 用户电子邮件地址填写 Apple Developer 账号邮箱。
   - 选择“存储到磁盘”，生成 `.certSigningRequest` 文件。
   - 不要删除当前 Mac 登录钥匙串中的私钥；之后下载的 `.cer` 必须和这把私钥配对。
3. 在 Apple Developer -> Certificates -> `+` 中创建 **Developer ID Application**。
   - 在 **Software** 分类中选择 `Developer ID Application`，其说明是“用于在 Mac App Store 之外分发的 App 代码签名”。
   - 不要选择 `Apple Development`、`Apple Distribution`、`Mac Development` 或 `Mac App Distribution`，它们用于开发调试、App Store 或测试分发。
   - 不要选择 `Mac Installer Distribution` 或 `Developer ID Installer`；它们用于安装包签名，不用于当前 DMG 内的 `.app` 签名。
   - 不要选择 `Apple Push Notification service SSL`；它是 APNs 服务器证书，不是桌面 App 签名证书。
   - Developer ID Application 页面如果出现平台选项，选择 `macOS`；不要选择 `All`。ARM 和 Intel 共用同一张证书。
   - 上传刚才生成的 CSR，完成后下载 `.cer`。
   - **⚠️ 这是有明确过期时间的文件。**在 Apple Developer 证书列表或下载后的证书详情中记录 `Expires` 日期。到期后必须重新创建证书、重新导出 `.p12` 并替换 GitHub Secret `APPLE_CERTIFICATE`；仅重新下载旧 `.cer` 或重新导出旧 `.p12` 不能续期。
4. 双击下载的 `.cer`，或打开“钥匙串访问”并将它拖到左侧“登录”钥匙串的“我的证书”中。
5. 在发布 Mac 执行：

```bash
security find-identity -v -p codesigning
```

必须看到类似下面的完整签名身份，并且展开后能看到对应私钥：

```text
Developer ID Application: Your Company (TEAMID)
```

如果只有证书没有私钥，说明证书不是在这台 Mac 上用当前钥匙串的 CSR 创建的；需要回原 Mac 导出 `.p12`，或者重新生成证书。

6. 在“钥匙串访问”中右键该 Developer ID 证书，选择“导出”，格式选择 `.p12`，设置导出密码。该 `.p12` 必须同时包含证书和私钥。
   - **⚠️ `.p12` 不是永不过期的备份。**它携带的 Developer ID Application 证书仍以证书的 `Expires` 日期为准；导出密码只是保护文件，不改变证书有效期。证书续期后必须重新导出新的 `.p12`。
7. 将 `.p12` 转为 GitHub Secret 所需的单行 Base64：

```bash
base64 -i DeveloperID.p12 | tr -d '\n'
```

8. 打开 [App Store Connect API Keys](https://appstoreconnect.apple.com/access/integrations/api)，进入“团队密钥”标签，点击 `+` 创建 **App Store Connect API Key**。
   - 名称可填写 `KN GitHub Notary`。
   - “访问”选择 **开发者**。
   - 这个权限用于 GitHub Actions 调用 `xcrun notarytool` 提交 macOS 公证，通常不需要选择“管理”或“财务”。
   - 点击“生成”后立即下载 `.p8`，同时记录页面上的 `Key ID` 和 `Issuer ID`。
   - `.p8` 只能下载一次；它不是 APNs `.p8`，也不是 Developer ID 证书。
   - **⚠️ 该 `.p8` 通常没有固定过期时间，但不是永久可靠凭据。**它被撤销、账号权限变化或账号失效后会立即不可用；原文件丢失不能补下载，只能新建 API Key。
9. 在 KN GitHub 仓库 Settings -> Secrets and variables -> Actions 添加：

| Secret | 内容 |
|---|---|
| `APPLE_CERTIFICATE` | `base64 -i DeveloperID.p12 | tr -d '\n'` 的输出 |
| `APPLE_CERTIFICATE_PASSWORD` | 导出 `.p12` 时设置的密码 |
| `KEYCHAIN_PASSWORD` | CI 临时钥匙串随机密码 |
| `APPLE_SIGNING_IDENTITY` | Developer ID Application 完整名称 |
| `APPLE_TEAM_ID` | Apple Team ID |
| `APPLE_NOTARY_KEY` | App Store Connect 公证 `.p8` 原文，包含 PEM 头尾 |
| `APPLE_NOTARY_KEY_ID` | Key ID |
| `APPLE_NOTARY_ISSUER_ID` | Issuer ID |

#### 2.1.1 GitHub Secrets 逐项创建清单

上表中的 8 项全部在 GitHub 仓库中分别创建，不要合并成一个 Secret：

```text
GitHub 仓库
-> Settings
-> Secrets and variables
-> Actions
-> New repository secret
```

进入页面后：

- 选择顶部的 **Secrets** 标签，不要选择 **Variables**。
- 在 **Repository secrets** 区域点击 **New repository secret**。
- 不要使用 **Environment secrets**，除非你另外配置了 GitHub Environment；本项目直接使用 Repository secrets。
- 8 个值都应出现在 Repository secrets 列表中，并显示锁头图标。

两者区别：

| 位置 | 用途 |
|---|---|
| `Secrets` -> `Repository secrets` | 加密保存证书、密码、`.p8` 和其他敏感值，本项目使用这里 |
| `Variables` | 明文保存普通配置，不用于保存密钥 |
| `Environment secrets` | 绑定特定 GitHub Environment 的 Secret，本项目暂不使用 |

按下面的名称逐项创建：

```text
APPLE_CERTIFICATE
APPLE_CERTIFICATE_PASSWORD
KEYCHAIN_PASSWORD
APPLE_SIGNING_IDENTITY
APPLE_TEAM_ID
APPLE_NOTARY_KEY
APPLE_NOTARY_KEY_ID
APPLE_NOTARY_ISSUER_ID
```

填写规则：

| Secret | 填写内容 |
|---|---|
| `APPLE_CERTIFICATE` | `base64 -i DeveloperID.p12 | tr -d '\n'` 的完整单行输出 |
| `APPLE_CERTIFICATE_PASSWORD` | 导出 `.p12` 时设置的密码 |
| `KEYCHAIN_PASSWORD` | `openssl rand -base64 32` 生成的随机字符串 |
| `APPLE_SIGNING_IDENTITY` | `Developer ID Application: jun zhao (8237PLJ8M5)` 这类完整签名名称 |
| `APPLE_TEAM_ID` | Apple Team ID，例如 `8237PLJ8M5` |
| `APPLE_NOTARY_KEY` | App Store Connect 公证 `.p8` 文件原文，必须包含 `BEGIN PRIVATE KEY` 和 `END PRIVATE KEY` |
| `APPLE_NOTARY_KEY_ID` | App Store Connect API Key 的 Key ID |
| `APPLE_NOTARY_ISSUER_ID` | App Store Connect 页面显示的 Issuer ID |

特别注意：

- 每个名称都点击一次 `New repository secret` 单独创建。
- `APPLE_CERTIFICATE` 必须是一整行 Base64，不能直接上传 `.p12` 文件。
- `APPLE_NOTARY_KEY` 粘贴 `.p8` 原文，不要 Base64，不要删除 PEM 头尾。
- `APPLE_CERTIFICATE_PASSWORD` 和 `KEYCHAIN_PASSWORD` 是两个不同密码。
- 配置完成后，GitHub Actions 日志中只能看到 `***`，不能看到这些 Secret 的真实值。
- 这些 Secret 不要写入 YAML、代码、Issue、Release Notes 或提交 Git。

#### 2.1.2 GitHub Actions 临时钥匙串密码

`KEYCHAIN_PASSWORD` 不是 Apple 提供的密码，也不是 Apple 账号密码。它只是 GitHub Actions 运行期间创建临时 macOS 钥匙串时使用的自定义密码。

在发布人 Mac 上生成随机值：

```bash
openssl rand -base64 32
```

复制输出内容，在 GitHub 仓库中打开：

```text
Settings -> Secrets and variables -> Actions -> New repository secret
```

新增：

```text
Name:   KEYCHAIN_PASSWORD
Secret: openssl 生成的随机字符串
```

它与其他密码的区别：

| 名称 | 用途 |
|---|---|
| `KEYCHAIN_PASSWORD` | GitHub Actions 临时钥匙串密码 |
| `APPLE_CERTIFICATE_PASSWORD` | 从钥匙串导出 `.p12` 时设置的密码 |
| Apple 账号密码 | 登录 Apple Developer/App Store Connect，不能填入以上 Secret |

不要把 `KEYCHAIN_PASSWORD` 写入代码、工作流文件或提交 Git。CI 结束后临时钥匙串会被删除。工作流虽然有默认值兜底，但生产发布必须显式配置该 Secret。

#### 2.2 App Store Connect 公证 `.p8`（文件 A）及丢失后的处理

注意：这是**文件 A**，只用于 GitHub Actions 调用 App Store Connect 公证服务。它不是 2.3 的 APNs 推送 `.p8`，两个文件不能互换。

> # ⚠️ 文件 A 的有效性
>
> **文件 A 通常没有 Apple 提供的固定 `Expires` 日期，但 Key 被撤销后立即失效。**不要把“没有到期日”理解为“可以永远不维护”。每次发布前应确认 API Key 仍存在、权限仍为“开发者”、Issuer ID 和 Key ID 与 GitHub Secrets 一致。`.p8` 只下载一次，丢失后只能按本节流程新建。

App Store Connect API 的 `.p8` 私钥在创建后只允许下载一次。Apple 不提供原文件再次下载；如果文件丢失，必须在 App Store Connect 中新建一个 API Key，重新下载新的 `.p8`，并同步替换 GitHub Secrets：

1. 进入 App Store Connect -> Users and Access -> Integrations -> Keys。
2. 点击 `+` 创建新的 API Key，选择与原来相同或足够的权限，点击生成。
3. 立即下载新的 `.p8` 并记录新的 `Key ID` 和原账号的 `Issuer ID`。
4. 在 GitHub Actions Secrets 中替换 `APPLE_NOTARY_KEY` 和 `APPLE_NOTARY_KEY_ID`；`APPLE_NOTARY_ISSUER_ID` 通常保持不变。
5. 先手动触发一次桌面构建，确认 `xcrun notarytool submit` 和公证流程成功。
6. 确认新 Key 工作正常后，再撤销旧的 App Store Connect API Key。不要在验证前撤销旧 Key。

#### 2.3 APNs 推送 `.p8`（文件 B）配置与丢失后的处理

注意：这是**文件 B**，只用于 Cloud 向 iPhone 推送通知。它不是 2.2 的 App Store Connect 公证 `.p8`，两个文件必须分别保存、分别记录 Key ID、分别配置。

> # ⚠️ 文件 B 的有效性
>
> **APNs Auth Key 的 `.p8` 通常没有固定 `Expires` 日期，但被撤销后立即失效。**它不能重新下载；丢失时必须新建 Key，并在 Cloud 服务器替换文件、Key ID 后重启服务。不要用文件 A 代替文件 B。

如果 Cloud 需要给 iOS 推送通知，还需要单独创建 **Apple Push Notifications service (APNs)** Key。它与上面的 `APPLE_NOTARY_KEY` 完全不同，不能互换。

1. 登录 <https://developer.apple.com/account/>。
2. 进入 **Certificates, Identifiers & Profiles -> Keys**，点击右上角 `+`。
3. 填写名称，例如 `KN APNs Production`，勾选 **Apple Push Notifications service (APNs)**。
4. 点击 Continue -> Register -> Download，立即保存下载的 `.p8`。
5. 记录该 Key 的 `Key ID`、Apple `Team ID`，并确认推送 Bundle ID，例如 `com.shark.kn`。
6. 将 `.p8` 放到服务器受保护目录，例如：

```bash
sudo install -d -o kn -g kn -m 0700 /opt/kn-cloud/secrets
sudo install -o kn -g kn -m 0600 AuthKey_APNs_<APNS_KEY_ID>.p8 /opt/kn-cloud/secrets/AuthKey_APNs_<APNS_KEY_ID>.p8
```

7. 在服务器 `/opt/kn-cloud/kn-cloud.env` 配置：

```ini
APNS_TEAM_ID=<Apple Team ID>
APNS_KEY_ID=<APNs Key ID>
APNS_KEY_FILE=/opt/kn-cloud/secrets/AuthKey_APNs_<APNS_KEY_ID>.p8
APNS_BUNDLE_ID=com.shark.kn
```

8. 重启实际使用 APNs 的 Cloud 服务，并查看日志确认启动时能读取 `.p8`；再通过真实设备验证推送。

APNs `.p8` 同样只能下载一次。文件丢失后不能找回原文件，只能重新创建一个 APNs Key：

1. 回到 Apple Developer -> Certificates, Identifiers & Profiles -> Keys。
2. 如果当前 Key 数量未达到上限，直接新建一个 APNs Key 并下载。
3. 如果已达到 APNs Key 上限，先确认哪些 Key 正在被其他服务使用，再撤销已经丢失或不再使用的旧 Key，然后创建新 Key。
4. 先部署并验证新 Key 推送成功，再撤销仍在使用的旧 Key。

建议保存为不同文件名，防止误覆盖：

```text
AuthKey_Notary_<APPLE_NOTARY_KEY_ID>.p8   # 文件 A：只保存在发布人受控环境/GitHub Secret
AuthKey_APNs_<APNS_KEY_ID>.p8             # 文件 B：只配置在 Cloud 服务器
```

撤销 Key 会立即使所有仍使用该 Key 的服务失效。`.p8` 文件、Key ID、Team ID 和 Bundle ID 都属于敏感配置，不能提交 Git、上传到 kn-admin、写入数据库或放进桌面 DMG。

### 3. 配置桌面端固定地址

编辑 KN 项目文件 `kn/desktop/src-tauri/runtime-config.json`：

```json
{
  "release_api_url": "https://api.example.com/api/v1/app-config/desktop-release",
  "cloud_ws_url": "wss://api.example.com/v1/ws",
  "cloud_http_url": "https://api.example.com"
}
```

这三项会打进 DMG。改 API/WSS 域名后，必须修改该文件、构建并发布更高桌面版本；旧域名不要立即关闭。

### 5. 配置服务器目录与环境变量

服务器执行：

```bash
sudo useradd --system --home /opt/kn-cloud --shell /usr/sbin/nologin kn
sudo install -d -o kn -g kn -m 0750 /opt/kn-cloud /opt/kn-cloud/logs
sudo install -d -o kn -g kn -m 0755 /opt/kn-cloud/releases /opt/kn-cloud/admin-web
```

创建服务器文件 `/opt/kn-cloud/kn-cloud.env`：

```ini
# MySQL 和 Redis
DB_URL=jdbc:mysql://127.0.0.1:3306/kn_cloud?useSSL=true&requireSSL=true&serverTimezone=UTC&rewriteBatchedStatements=true
DB_USER=kn_cloud
DB_PASS=<数据库密码>
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASS=<Redis密码>

# API、WSS、Admin 使用的业务密钥
JWT_SECRET=<至少32字节随机字符串>
REDEEM_AES_KEY=<兑换码AES密钥>
ADMIN_JWT_SECRET=<管理员JWT随机密钥>
ADMIN_TOTP_ENCRYPTION_KEY=<管理员TOTP加密密钥>

# 邮件
MAIL_USERNAME=<SMTP用户名>
MAIL_PASSWORD=<SMTP密码>
MAIL_FROM=noreply@example.com

# 域名和桌面发布
KN_BASE_URL=https://api.example.com
KN_WS_ALLOWED_ORIGINS=https://api.example.com
KN_RELEASE_DOWNLOAD_BASE_URL=https://api.example.com/releases
KN_RELEASE_WEBSITE_ORIGIN=https://www.example.com
KN_RELEASE_ROOT=/opt/kn-cloud/releases
KN_MIN_PROTOCOL_VERSION=1

# 首个管理员：首次登录成功后删除后三项并重启 kn-admin-api
ADMIN_BOOTSTRAP_EMAIL=admin@example.com
ADMIN_BOOTSTRAP_PASSWORD=<首次强密码>
ADMIN_BOOTSTRAP_TOTP_SECRET=<Base32 TOTP密钥>
ADMIN_CORS_ORIGIN=https://admin.example.com
```

```bash
sudo chown kn:kn /opt/kn-cloud/kn-cloud.env
sudo chmod 600 /opt/kn-cloud/kn-cloud.env
```

新服务器直接执行 `kn-cloud/deploy/init.sql`。已有数据库只执行它最后 `kn_desktop_release` 的建表和索引区块一次；以后升级只新增 `ALTER` 脚本。

---

## 二、发布桌面 KN：从打包到上传 kn-admin

这是每次正式发桌面版本唯一需要照做的连续流程。

### 1. 修改版本和桌面配置

在 KN 项目中只修改根目录 `Cargo.toml` 的 `[workspace.package] version`，例如：

```toml
[workspace.package]
version = "1.2.0"
```

Agent、桌面 App、Tauri 打包和 GitHub Actions 都从这个版本读取；官网版本展示由 kn-cloud 仓库维护。

确认 `desktop/src-tauri/runtime-config.json` 已填对 API/WSS 域名。

### 2. 本地检查

```bash
cd kn
cargo test --workspace
cd desktop && npm test && npx tsc --noEmit && npm run build
```

### 3. 合并 main 并触发候选包

```bash
git checkout main
git pull origin main
git tag -a v1.2.0 -m "v1.2.0"
git push origin main v1.2.0
```

在 GitHub Actions -> `Build Desktop App` 等待成功，下载 artifact：`release-candidate-v1.2.0`。它必须包含：

1. ARM DMG
2. Intel DMG
3. `release-notes.md`

### 4. 上传到 kn-admin

访问 `https://admin.example.com`，登录后进入“桌面版本发布” -> “上传候选包”。

| 页面字段 | 从哪里取值 |
|---|---|
| 版本 | 本次发布版本，例如 `1.2.0` |
| Agent 版本 | 与桌面版本相同，例如 `1.2.0` |
| 最低协议版本 | `minProtocolVersion`，当前为 `1` |
| Release Notes | 从 `release-notes.md` 复制 |
| ARM DMG | artifact 中 ARM DMG |
| Intel DMG | artifact 中 Intel DMG |

点击“计算哈希并创建草稿”。Admin 会保存文件到 staging 目录，计算两个 DMG 的 SHA-256，再写入数据库；不需要公钥、私钥、Manifest 或签名文件。

### 5. 草稿验收后发布

在 ARM 和 Intel 真机分别完成：首次安装、Gatekeeper、公证、绑定、远控、断网重连、升级后的 Agent 替换、解绑后不可远控。

确认后在 kn-admin 草稿列表点击“发布”。服务器会把文件原子移动到：

```text
/opt/kn-cloud/releases/<版本>/
```

并切换唯一公开版本指针。

### 7. 发布后检查与回滚

检查官网 ARM/Intel 下载、桌面“检查更新”、SHA-256、`spctl -a -vv <App>`。观察 24 小时。

有问题时在 kn-admin 对旧正式版本点击“回滚到此版”。不要删除或覆盖已发布 DMG。

---

## 三、部署 Cloud REST API 项目

项目目录：`kn-cloud/kn-cloud-api/`。

### 1. 改哪里

- 生产配置：`kn-cloud/kn-cloud-api/src/main/resources/application-prod.yml`
- 实际值：服务器 `/opt/kn-cloud/kn-cloud.env`
- 服务定义：`kn-cloud/deploy/kn-cloud-api.service`

YAML 内已有中文注释。日常只改环境文件，不把密码写进 YAML。

API 域名相关变量：`KN_BASE_URL`、`KN_RELEASE_DOWNLOAD_BASE_URL`、`KN_RELEASE_WEBSITE_ORIGIN`。

### 2. 构建

```bash
cd kn-cloud
mvn -pl kn-cloud-api -am clean package
```

### 3. 上传和重启

```bash
API_JAR=$(find kn-cloud-api/target -maxdepth 1 -type f -name 'kn-cloud-api-*.jar' ! -name '*-sources.jar' ! -name '*-original.jar')
test "$(printf '%s\n' "$API_JAR" | sed '/^$/d' | wc -l | tr -d ' ')" = 1
scp "$API_JAR" user@server:/tmp/kn-cloud-api.jar
scp deploy/kn-cloud-api.service user@server:/tmp/
ssh user@server
sudo install -o kn -g kn -m 0644 /tmp/kn-cloud-api.jar /opt/kn-cloud/kn-cloud-api.jar
sudo install -m 0644 /tmp/kn-cloud-api.service /etc/systemd/system/kn-cloud-api.service
sudo systemctl daemon-reload
sudo systemctl restart kn-cloud-api
sudo systemctl status kn-cloud-api --no-pager
journalctl -u kn-cloud-api -n 100 --no-pager
```

---

## 四、部署 Cloud WSS 项目

项目目录：`kn-cloud/kn-cloud-ws/`。

### 1. 改哪里

- 生产配置：`kn-cloud/kn-cloud-ws/src/main/resources/application-prod.yml`
- 实际值：服务器 `/opt/kn-cloud/kn-cloud.env`
- 服务定义：`kn-cloud/deploy/kn-cloud-ws.service`

关键变量：`JWT_SECRET` 必须和 REST API 相同；`KN_WS_ALLOWED_ORIGINS` 填允许的 HTTPS Origin；`KN_MIN_PROTOCOL_VERSION` 控制旧 Agent 是否必须升级。

### 2. 构建、上传和重启

```bash
cd kn-cloud
mvn -pl kn-cloud-ws -am clean package
WS_JAR=$(find kn-cloud-ws/target -maxdepth 1 -type f -name 'kn-cloud-ws-*.jar' ! -name '*-sources.jar' ! -name '*-original.jar')
test "$(printf '%s\n' "$WS_JAR" | sed '/^$/d' | wc -l | tr -d ' ')" = 1
scp "$WS_JAR" user@server:/tmp/kn-cloud-ws.jar
scp deploy/kn-cloud-ws.service user@server:/tmp/
ssh user@server
sudo install -o kn -g kn -m 0644 /tmp/kn-cloud-ws.jar /opt/kn-cloud/kn-cloud-ws.jar
sudo install -m 0644 /tmp/kn-cloud-ws.service /etc/systemd/system/kn-cloud-ws.service
sudo systemctl daemon-reload
sudo systemctl restart kn-cloud-ws
sudo systemctl status kn-cloud-ws --no-pager
journalctl -u kn-cloud-ws -n 100 --no-pager
```

---

## 五、部署 kn-admin API 和后台网页

项目目录：`kn-cloud/kn-admin-api/` 和 `kn-cloud/kn-admin-web/`。

### 1. 改哪里

- API YAML：`kn-cloud/kn-admin-api/src/main/resources/application.yml`、`application-prod.yml`
- API 实际值：服务器 `/opt/kn-cloud/kn-cloud.env`
- API service：`kn-cloud/deploy/kn-admin-api.service`
- 前端不写 API 域名，Nginx 将同域 `/api/admin/v1/` 转给 Admin API。

桌面发布必须配置：`KN_RELEASE_ROOT`、`KN_RELEASE_DOWNLOAD_BASE_URL`。不需要配置发布公钥或私钥。

### 2. 构建

```bash
cd kn-cloud
mvn -pl kn-admin-api -am clean package
cd kn-admin-web
npm ci
npm run build
```

### 3. 上传和重启

```bash
ADMIN_JAR=$(find kn-admin-api/target -maxdepth 1 -type f -name 'kn-admin-api-*.jar' ! -name '*-sources.jar' ! -name '*-original.jar')
test "$(printf '%s\n' "$ADMIN_JAR" | sed '/^$/d' | wc -l | tr -d ' ')" = 1
scp "$ADMIN_JAR" user@server:/tmp/kn-admin-api.jar
scp -r kn-admin-web/dist/. user@server:/tmp/kn-admin-web/
scp deploy/kn-admin-api.service user@server:/tmp/
ssh user@server
sudo install -o kn -g kn -m 0644 /tmp/kn-admin-api.jar /opt/kn-cloud/kn-admin-api.jar
sudo rm -rf /opt/kn-cloud/admin-web/*
sudo cp -a /tmp/kn-admin-web/. /opt/kn-cloud/admin-web/
sudo chown -R kn:kn /opt/kn-cloud/admin-web
sudo install -m 0644 /tmp/kn-admin-api.service /etc/systemd/system/kn-admin-api.service
sudo systemctl daemon-reload
sudo systemctl restart kn-admin-api
sudo systemctl status kn-admin-api --no-pager
```

---

## 六、手动部署官网项目

官网代码、构建与部署步骤已迁移至 `kn-cloud` 仓库。Cloud CORS 的 `KN_RELEASE_WEBSITE_ORIGIN` 必须与官网完整 origin 一致；官网 Nginx 配置在 `kn-cloud/deploy/nginx.conf`。官网不通过 GitHub Actions 发布。

---

## 七、部署 Nginx 和 HTTPS

模板文件：`kn-cloud/deploy/nginx.conf`。复制到服务器 Nginx 的实际启用配置后替换：

1. 所有 `api.knshark.com` 为 API 域名。
2. 所有 `admin.knshark.com` 为 Admin 域名。
3. 官网使用 `knshark.com` / `www.knshark.com`，站点文件目录为 `/var/www/kn-site`。
4. 三组 `ssl_certificate`、`ssl_certificate_key` 为 Certbot 生成的真实路径。
5. `/releases/` 的 `alias` 必须等于 `KN_RELEASE_ROOT`，通常为 `/opt/kn-cloud/releases/`。

> # ⚠️ Nginx HTTPS 证书会过期
>
> `ssl_certificate` 指向的证书文件有明确的 `Not After` 日期；私钥文件本身没有“到期日”，但必须和当前证书匹配。不要把 HTTPS 证书和 Apple 的 Developer ID 证书、App Store Connect `.p8` 混为一谈。

如果服务器使用 Certbot/Let's Encrypt，首次申请并启用自动续期：

```bash
sudo certbot certificates
sudo certbot renew --dry-run
systemctl list-timers | grep certbot
```

续期成功后确认 Nginx 重新加载了新证书：

```bash
sudo nginx -t
sudo systemctl reload nginx
echo | openssl s_client -connect api.example.com:443 -servername api.example.com 2>/dev/null \
  | openssl x509 -noout -subject -issuer -dates
```

至少检查 `api`、`admin` 和官网域名。输出中的 `notAfter` 必须晚于当前日期；如果 Certbot 没有自动 reload Nginx，应补充 deploy hook 或续期后手动 reload。

应用前检查：

```bash
sudo nginx -t
sudo systemctl reload nginx
curl -I 'https://api.example.com/api/v1/app-config/desktop-release?platform=darwin-aarch64&currentVersion=0.0.0'
```

---

## 八、域名变更时按这张表处理

| 改动 | 必改位置 | 后续动作 |
|---|---|---|
| API 域名 | `kn-cloud.env` 的 `KN_BASE_URL`、`KN_WS_ALLOWED_ORIGINS`、`KN_RELEASE_DOWNLOAD_BASE_URL`；Nginx API；`desktop/src-tauri/runtime-config.json`；官网的 kn-cloud 配置 | 重启 API/WSS/Admin、重载 Nginx、重建官网、发更高桌面版本 |
| Admin 域名 | Nginx Admin；`ADMIN_CORS_ORIGIN` | 重启 Admin API、重载 Nginx |
| 官网域名 | Nginx 官网；`KN_RELEASE_WEBSITE_ORIGIN` | 重启 Cloud API、重载 Nginx |

API 改域名时旧域名要继续提供更新接口、WSS 和下载文件，直到旧桌面版本不再使用。

---

## 九、禁止事项

- 不把 `.p8`、`.p12`、密码、JWT、Redis 密码、数据库密码、device token 提交 Git。
- 不用 GitHub Actions 部署 Cloud、Admin 或官网。
- 不直接写发布数据库记录，不覆盖已发布 DMG，不跳过 SHA-256 校验。
